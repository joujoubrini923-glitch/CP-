/* ============================================================
   ApexJudge Academy — cloud storage config
   ------------------------------------------------------------
   Leave both values empty ('') to run in local-only mode: each
   browser keeps its own copy of the data (the original behavior).

   To make Admin changes appear automatically for every visitor:
   1. Go to https://jsonbin.io and create a free account.
   2. Click "Create Bin", paste in the contents of
      assets/starter-data.json, and save it.
   3. Copy the Bin's ID (shown in the URL / bin details) into
      BIN_ID below.
   4. Go to Account → API Keys, copy your "X-Master-Key" into
      API_KEY below.
   5. Save this file and re-upload it to your host. That's it —
      no other file needs to change.
   ============================================================ */

const CLOUD_CONFIG = {
  BIN_ID: '6a5e95f9f5f4af5e29a9c3be',   // e.g. '65f1a2b3c4d5e6f7a8b9c0d1'
  API_KEY: '$2a$10$iiu/HTKlrUDmL9NRfWIHreVCUNj9fS9779q8oOyAq87INdzuyP3U2',  // e.g. '$2b$10$abcdefghijklmnopqrstuvwx...'
};
